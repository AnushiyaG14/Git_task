# Git Task – Python Utility

This repository contains a simple Python utility script created as part of a Git assessment.

## Features
- Adds two numbers using a reusable function
- Demonstrates clean Git practices
- Uses `.gitignore` to avoid committing unwanted files

## How to Run
```bash
python sum_two_numbers.py
